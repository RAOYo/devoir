Algorithme : les diviseurs d'un nombre

Variables:  n ,i : entiers ;

Début
    AFFICHER("Entrer le nombre n:");
    SAISIR(n);
    AFFICHER("les diviseurs de" ,n, "sont:");
    Pour(i<-1 ; i<=n ; i++)
        Si(n%i=0)alors
        	AFFICHER(i);
	finsi
	
    finPour

Fin
